#!/bin/sh

set -e

if [ ! -e /etc/services ] || \
    ! grep -Eq '^[[:space:]]*krb5_admin[[:space:]]+2666/tcp([[:space:]]|$)' \
    /etc/services; then
	echo 'krb5_admin 2666/tcp' >> /etc/services
fi

#DEBHELPER#
